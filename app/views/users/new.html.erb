<h1>user login gate</h1>
<%= form_for @user, :html => {:class => "form login-form"} do |f| %>

  <%= f.label(:username, "user name") %>
  <%= f.text_field :username %>
  <%= f.label(:password, "password") %>
  <%= f.password_field(:password)%>
  <%= f.submit "submit" %>
<% end %>
